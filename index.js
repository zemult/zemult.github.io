window.onload = function () {
  console.log("No crash");
};

const btn = document.getElementById('inv')

btn.addEventListener('click', () => {
    alert('Surreal is currently still in development! Check back soon!')
})